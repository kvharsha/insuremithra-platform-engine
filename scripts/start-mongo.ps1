Write-Host "Start-mongo helper (Windows) - this script is a no-op when using the GitHub Action that starts MongoDB"
Write-Host "If you want to run Mongo locally, install MongoDB locally or use Docker and connect using MONGODB_URI"
